@model InStaging.MvcApp.Models.TicketIndexVM
@{
    ViewBag.ActiveNav = "Tickets";
    ViewBag.Title = "Tickets";
}
@section navbar{

}
<div class="panel">
    <div class="navbar">
        <div class="ml-auto">
            <button class="btn btn-sm btn-outline-primary my-2 my-sm-0">Board</button>
            <a href="@Url.Action("Add", "Ticket")" class="btn btn-sm btn-outline-primary my-2 my-sm-0">Create Ticket</a>
        </div>
    </div>
    <table class="table">
        <thead>
            <tr>
                <th>Date</th>
                <th>Details</th>
                <th class="text-center">Actions</th>
            </tr>
        </thead>
        <tbody>
        </tbody>
    </table>

</div>